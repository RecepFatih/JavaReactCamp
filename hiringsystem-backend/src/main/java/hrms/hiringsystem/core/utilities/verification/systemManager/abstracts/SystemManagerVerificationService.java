package hrms.hiringsystem.core.utilities.verification.systemManager.abstracts;

import hrms.hiringsystem.core.utilities.results.Result;

public interface SystemManagerVerificationService {
	Result verify(String systemManager);
}
